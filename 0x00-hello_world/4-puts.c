#include <stdio.h>

/*
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char MyMessage[] = "Programming is like building a multilingual puzzle";
	puts(MyMessage);
	return 0;
}
